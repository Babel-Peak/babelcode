---
"kilo-code": minor
---

Show agent modes above AI responses, add a viewport indicator line under the task timeline that follows chat scrolling with user queries highlighted in a distinct color, and add a default reasoning selector per mode in the Models settings so each mode's reasoning effort applies in chat.
